name: flutter_profile_picker_e_resize

description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.16.1 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:                
    sdk: flutter
  font_awesome_flutter: ^9.2.0          
  get: ^4.6.1                           
  get_storage: ^2.0.3                   
  google_fonts: ^2.2.0                  
  image_picker: ^0.8.4+6                 
  image_cropper: ^1.4.1
  flutter_native_image: ^0.0.6+1        
  cupertino_icons: ^1.0.2           
  
dev_dependencies:

  flutter_lints: ^1.0.0
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true

  assets:
    - assets/imagens/
